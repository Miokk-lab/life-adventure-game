import { jsx as g, Fragment as w } from "react/jsx-runtime";
import c, { useMemo as y, useState as h, useRef as v, useEffect as m } from "react";
const p = (r) => r == null || typeof r == "boolean" ? 0 : typeof r == "string" || typeof r == "number" ? String(r).length : Array.isArray(r) ? r.reduce((t, u) => t + p(u), 0) : c.isValidElement(r) ? p(r.props.children) : 0, a = (r, t, u = "tw") => {
  if (t.stopped || r == null || typeof r == "boolean") return null;
  if (typeof r == "string" || typeof r == "number") {
    const n = String(r);
    if (t.remaining >= n.length)
      return t.remaining -= n.length, n;
    const e = n.slice(0, t.remaining);
    return t.remaining = 0, t.stopped = !0, e;
  }
  if (Array.isArray(r))
    return r.map((n, e) => /* @__PURE__ */ g(c.Fragment, { children: a(n, t, `${u}-${e}`) }, `${u}-${e}`));
  if (c.isValidElement(r)) {
    const n = r.props, e = a(n.children, t, u);
    return c.cloneElement(r, void 0, e);
  }
  return null;
}, b = ({ children: r, speed: t = 90, trigger: u, autoPlay: n = !0, onDone: e }) => {
  const i = y(() => p(r), [r]), [f, s] = h(n ? 0 : i), l = v(null);
  return m(() => {
    if (l.current && window.clearInterval(l.current), !n) {
      s(i);
      return;
    }
    if (s(0), i !== 0)
      return l.current = window.setInterval(() => {
        s((o) => o >= i ? (l.current && window.clearInterval(l.current), o) : o + 1);
      }, t), () => {
        l.current && window.clearInterval(l.current);
      };
  }, [i, t, u, n]), m(() => {
    i > 0 && f >= i && e?.();
  }, [f, i]), /* @__PURE__ */ g(w, { children: a(r, { remaining: f, stopped: !1 }) });
};
b.displayName = "Typewriter";
export {
  b as Typewriter
};
